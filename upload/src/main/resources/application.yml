spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/cicd
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
    generate-ddl: true
file:
  upload:
    location: /Users/hyunho/upload/

logging:
  level:
    org.hibernate.SQL: trace
    org.hibernate.type: trace

file_dir: "/Users/file/upload/"